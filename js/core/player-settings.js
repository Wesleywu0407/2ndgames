// Shared first-person controller tuning.
// Keep movement numbers out of main.js so game feel can be adjusted in one place.

export const DOUBLE_TAP_RUN_MS = 280;

export const PLAYER = {
  height: 1.62,
  walkSpeed: 2.65,
  runSpeed: 4.15,
  accel: 12,
  decel: 14,
  strafeMul: 0.88,
  radius: 0.34,
  interactDot: 0.42
};
